@startuml
class DehydrationLevel {
    - appearance: String
    - mucous: String
    - tears: String
    - eyes: String
    + getPoints(): Int
    - evaluateAppearance(): Int
    - evaluateMucous(): Int
    - evaluateTears(): Int
    - evaluateEyes(): Int
}

class DehydrationTreatment {
    + calculateWaterIntake(weight: Double, age: Int, degreeOfDehydration: Int): Double
    + calculateWaterIntakeInGlasses(weight: Double, age: Int, degreeOfDehydration: Int): Int
    - getMultiplier(degreeOfDehydration: Int, age: Int): Double
}

class Electrolites {
    - element: String
    - age: Int
    + getInfo(): String
    - getSolution(element: String): String
    - getInjections(age: Int): String
}

class WaterBalance {
    - weight: Double
    - age: Int
    + calculateDailyWaterIntake(): Double
    + calculateDailyWaterIntakeInGlasses(): Int
}

class DehydrationFragment {
    - _binding: FragmentDehydrationBinding?
    + onCreateView()
    + onDestroyView()
}

class DehydrationViewModel {
    - _text: MutableLiveData<String>
    + text: LiveData<String>
    + calculateDehydration(appearanceValue: Int, eyesValue: Int, mucousValue: Int, tearsValue: Int): String
}

class DehydrationTreatmentFragment {
    - _binding: FragmentDehydrationTreatmentBinding?
    + onCreateView()
    + onDestroyView()
}

class DehydrationTreatmentViewModel {
    + calculateDehydrationTreatment(selectedDegree: Int, selectedAge: Int, selectedWeight: Int): String
}

class ElectrolitesFragment {
    - _binding: FragmentElectrolitesBinding?
    + onCreateView()
    + onDestroyView()
}

class ElectrolitesViewModel {
    + calculateElectrolites(selectedElement: String, selectedAge: Int): String
}

class WaterFragment {
    - _binding: FragmentWaterBinding?
    + onCreateView()
    + onDestroyView()
}

class WaterViewModel {
    + calculateWaterBalance(age: Int, weight: Int): String
}

DehydrationFragment --> DehydrationViewModel
DehydrationViewModel --> DehydrationLevel

DehydrationTreatmentFragment --> DehydrationTreatmentViewModel
DehydrationTreatmentViewModel --> DehydrationTreatment

ElectrolitesFragment --> ElectrolitesViewModel
ElectrolitesViewModel --> Electrolites

WaterFragment --> WaterViewModel
WaterViewModel --> WaterBalance
@enduml