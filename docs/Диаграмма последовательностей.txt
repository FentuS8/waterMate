@startuml
title Water Intake Calculation Sequence Diagram

actor User

User -> WaterFragment: Открывает WaterFragment
WaterFragment -> WaterViewModel: Создает WaterViewModel
WaterViewModel -> WaterBalance: Создает WaterBalance
WaterViewModel -> WaterBalance: Вызывает calculateDailyWaterIntake()
WaterBalance -> WaterViewModel: Возвращает daily water intake
WaterViewModel -> User: Отображает daily water intake

@enduml